spring.application.name=Zusteller-Position-Tracker
# read while application context is being loaded: See Reference manuel of Spring
# now we want to create two instances of PositionTracking Microservices
#eureka.instance.instanceId=${spring.application.name}:${random.value}